extends layout

block content
  h2 クイズ部分  
  #max(data-max=max)
  h2#rank(data-rank=rank) #{rank} ランクが選択されています
  h4#Q.text-center
  #countdown-bar
  p#quiz-area

   
  button#ans-btn 答える！
  .popup
    #user-input.rounded
      h4 回答
      h5#user-input-text
    #ans-area.rounded
      #ans-input
  
