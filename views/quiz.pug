extends layout

block content  
  #max(data-max=max)
  ul 参加者
    li #{userName}
  h2#rank(data-rank=rank) #{rank} ランクが選択されています
  h4#Q.text-center
  #countdown-bar
  p#quiz-area

   
  button#ans-btn 答える！
  .popup
    #user-input.rounded
      h4 回答
      p#answer-limit
      h5#user-input-text
    #ans-area.rounded
      #ans-input
  
